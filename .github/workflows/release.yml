name: Release

on:
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  tag:
    runs-on: ubuntu-latest
    outputs:
      new_tag: ${{ steps.tag_version.outputs.new_tag }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Bump version and push tag
        id: tag_version
        uses: mathieudutour/github-tag-action@v6.2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

  build:
    needs: tag
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch: [linux_amd64, darwin_amd64, darwin_arm64]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bazelisk
        uses: bazelbuild/setup-bazelisk@v3

      - name: Mount Bazel Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/bazel-disk-cache
            ~/.cache/bazel-repository-cache
          key: ${{ runner.os }}-bazel-${{ matrix.arch }}-${{ hashFiles('MODULE.bazel.lock', 'go.sum') }}
          restore-keys: |
            ${{ runner.os }}-bazel-${{ matrix.arch }}-

      - name: Build Binary
        run: |
          bazel build --disk_cache=~/.cache/bazel-disk-cache --repository_cache=~/.cache/bazel-repository-cache --platforms=@rules_go//go/toolchain:${{ matrix.arch }} //cmd/app:server
          cp bazel-bin/cmd/app/server_/server webapp-${{ matrix.arch }} || cp bazel-bin/cmd/app/server webapp-${{ matrix.arch }}

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: webapp-${{ matrix.arch }}
          path: webapp-${{ matrix.arch }}

  release:
    needs: [tag, build]
    runs-on: ubuntu-latest
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.tag.outputs.new_tag }}
          files: artifacts/*
          generate_release_notes: true
